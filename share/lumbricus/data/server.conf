port = "12499"
name = "Another unconfigured servername"
max_players = "4"

//Lag simulation: outgoing packets will be delayed by (sim_lag +- sim_jitter) ms
sim_lag = "0"
sim_jitter = "0"

worm_hp = "200"
worm_count = "4"

//Announce info, one section each announce method
//Default: LAN+Internet+More internet
announce {
    lan {
        //port = "12345"
    }
    php {
        //duplicated in connect_gui.conf
        script_url = "http://lumbricus.ath.cx/announce/announce.php"
    }
}
