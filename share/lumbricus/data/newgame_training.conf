
level = "generate"

gamemode {
    plugin = "gm_training"
}

teams {
    Trainee {
        id = "0"
        color = "red"
        member_names {
            "Idiot"
        }
        power = "200"
    }
}

managment {
    enable_demo_recording = "true"
}

gfx {
    config = "wwp.conf"
    waterset = "blue"
}

weapons {
    //suppress warning
    //the weapons are actually added by the GM plugin
    default = "empty"
}

plugins {
    messages {
        //--no annoying messages; plugin has its own messages mostly?
        no_default_msgs = "true"
    }
    //"statistics"
    //"persistence"
    "ws_lua"
    "debug"
}

gamestate {

}
