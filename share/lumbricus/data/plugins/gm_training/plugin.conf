dependencies {
    "worms_shared"
}

locales {
    training = "locale"
}

resources {
    animations {
        target {
            type = "list"
            pattern = "img/target.*.png"
        }
    }
}

sequences {
    s_target {
        normal = "target"
    }
}

modules {
    "main.lua"
    "program.lua"
}
