dependencies {
    "worms_shared"
}

locales {
    training = "locale"
}

resources {
    animations {
        target {
            type = "list"
            pattern = "img/target.*.png"
        }
    }
}

sequences {
    s_target {
        normal = "target"
    }
}

modules {
    "target.lua"
    "program.lua"
}
