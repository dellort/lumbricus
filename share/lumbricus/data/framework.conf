drivers {
    base = "sdl"
    font = "freetype"
    //use sound = "null" to disable sound
    sound = "null"
}
sdl {
    open_gl = "true"
    enable_caching = "true"
    mark_alpha = "false"
    rle = "true"
    lowquality = "false"
    gl_debug_wireframe = "false"
    //"x y" position or "center" ; OS default if empty / no value set
    //(at least on Win, pos excludes window border / decorations)
    //xxx currently only works on Windows
    window_pos = "center"
}
freetype {
    font_packer = "true"
}
fmod {
    //channels = "28"
}
sound {
    master_volume = "0.8"
    volume0 = "1.0"   //sfx
    volume1 = "1.0"   //music
}