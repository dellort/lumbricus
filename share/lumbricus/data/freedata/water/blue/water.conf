//now specified somewhere else
//color = "r=0.2, g=0.2, b=0.5"

resources {
    animations {
        water_waves {
            type = "list"
            pattern = "out*.png"
            frametime = "25"
        }
    }
}
