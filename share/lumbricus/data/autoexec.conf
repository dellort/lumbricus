//each entry is a string that's executed like on the in-game command line

//"spawn levelpreview"
//"spawn game"
//"spawn testtask"
//"spawn enet_test"
"spawn welcomescreen"

//executed last, so that the window has highest z-order
"spawn localeswitch check"
