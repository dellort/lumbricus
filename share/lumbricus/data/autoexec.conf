//each entry is a string that's executed like on the in-game command line

//"spawn levelpreview"
//"spawn game"
//"spawn testtask"
//"spawn enet_test"
"spawn welcomescreen"
"spawn localeswitch check"  //xxx no always-on-top, so needs to be last line
